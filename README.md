# com-etzhayyim-ie-flow

Shared information-energy flow metrics, gate adapter, lifecycle, ledger, control, and reward
library. Canonical repository metadata is `library.edn`.

The actor-coupled scoreboard is intentionally not part of this library.

Run `./run_tests.sh` from a standalone checkout.
